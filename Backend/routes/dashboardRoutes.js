import { Router } from 'express';

const router = Router();

// Example placeholder route – replace with real dashboard logic
router.get('/', (req, res) => {
  res.json({ message: 'Dashboard route is operational' });
});

export default router;
